import React from "react";

const BollCentro = document.querySelector(".Icone-Centro")
BollCentro.addEventListener('')

const updateBollposition = (e) => {

    const mouseX = e.clientX; 
    const mouseY = et.clientY;
    const centerX = window.innerWidth / 2;
    const centerY = window.innerHeight / 2;
    




}

export default updateBollposition
